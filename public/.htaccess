# Web-root configuration.
#
# Two jobs only: route everything through the front controller when URL rewriting is
# available, and harden the directory. When rewriting is NOT available the front controller
# is still reachable as /index.php?r=/path (the Request object resolves that form), but the
# links the application generates are clean URLs - see the URL section of the install
# runbook for what a host without rewriting needs (ADR-0002, O-20).

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Do not rewrite real files or directories (static assets keep working).
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Everything else goes to the front controller.
    RewriteRule ^ index.php [L]
</IfModule>

# Never execute anything inside the public tree except the front controller.
# (Uploaded files and generated assets live outside the web root; see the backend rule.)
<FilesMatch "\.(php|phtml|phar)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
<Files "index.php">
    <IfModule mod_authz_core.c>
        Require all granted
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Allow from all
    </IfModule>
</Files>

# The installer is the second (and last) exception, for hosts without shell access. It answers 404 as
# soon as config/config.php exists, so the window in which it is reachable is the window in which the
# site is not yet installed. Adding a third PHP file to this directory means editing this list - see
# DeploymentContractTest, which fails if public/ contains a PHP file that is not in it.
<Files "install.php">
    <IfModule mod_authz_core.c>
        Require all granted
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Allow from all
    </IfModule>
</Files>

# Directory listings are never helpful and can leak file names.
Options -Indexes

<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
</IfModule>
